# Interim report 18 (2026-09-01): audit 2 response, P0 hardening, Multivariate first certification

Branch: `dev/proofs-v1` (from `dev/semantics-v1` at 5918c8e).  Roadmap: DESIGN §24.

## Audit 2

Response in `docs/audits/2026-09-01-audit-2-response.md`.  All five concerns accepted;
verified independently on our side (the auditor's 236-overlap and seven-theorem figures
reproduce exactly).

## P0 — hardening (done)

- CI clones `mgwiki/Megalodon` (was `ai4reason/...`; this is what kept CI red).
- `tools/update.sh` gates `check_nativeproof.sh` and `cert_finalize.py` fatally.
- README repairs: stale "every theorem is Admitted", split table, 62/67, pilot-round-6 text.
- `cert_finalize.py` records imported proofs for model-proved theorems too:
  **847 fully_proved, 7 under both statuses** (`model_and_imported`); the 57/58 count
  difference was INFINITY_AX (both discharges, still `pending_mapping`).

## P1 — Multivariate statements (first certification pass)

17 526 theorems, 17 138 public.  The literal/cert layer generates in ~22 s.  New work to
make the base check: `hl_ty_group_nonempty` (trivial group witness; via local
`hl_FST_pair2`/`hl_SND_pair2`/`grp_choose_unique` helpers from `hl_select_eq`),
`hl_ty_multivector_nonempty` (empty index set), per-profile carrier slots in
`tools/check_cert.sh`, and a typing-lemma naming fix (`hl_<c>_in1` when HOL has a
constant `<c>_in` — nine topology constants; core and standard outputs byte-identical).

**Result: 3 311 of 17 138 public statements `transport_checked`; 48 shards, 0 failures,
0 mismatches.**  Remaining: 13 827 `bridge_unsupported` behind 515 missing compat lemmas
(388 `pending_mapping` are mostly quarantined construction internals; the genuinely
unmapped constants are `group_sum`/`group_product`).

## Compat campaign design

Prototype `hl_vec_compat` (first entry of `mglib/literal/compat_multivariate.mg`) checks
first try and unlocks six theorems.  All 515 required lemmas equate a literal constant
with an *auto-translated native definition* — two translations of the same HOL defining
equation — so the campaign will be run by a **generator-emitted auto-compat pass**
(statement synthesis already exists for `_compat_required.mg`; the proof comes from the
bridge deriver on the defining equation).  That emitter is the next implementation
milestone; per-lemma hand work does not scale (most blocked theorems need several lemmas
at once).

## Next

DESIGN §24.2(2): the auto-compat emitter; then §24.3 (skeleton-following proof synthesis
over the 236-theorem ground truth) in parallel.
