# Interim report 19 (2026-09-01): leaf-guided native proof import (N5)

Goal (DESIGN §24.3): every imported (fully_proved) theorem — 847 on core today, later the
Multivariate rounds — carries a *nice native Megalodon proof*.  Baseline: 236 of the 847
already have generated native proofs; 608 targets remain, all with recorded kernel proofs
on disk (round 8, 113 MB).

## What was built

- **`mglib/native/logic.mg`** — the native `<->`-congruence toolkit (connectives, bounded
  and subset quantifiers, prop quantifier, `iff_true_intro`/`iff_false_intro`, equality
  helpers), composed into the native contexts only (no clash with the literal bridge).
- **`Nativeproof.iff_congruence`** — structural congruence proof terms between similar
  props, descending shared connectives/binders, closing leaves from premise instances or
  equality-instance Leibniz motives.
- **`Nativeproof.prove_via_rewrites`** — rewrite-normalization of a goal to `True` with
  the recorded proof leaves as rules: nested clause splitting, plain facts as `p <-> True`,
  candidate lists with chain-level retry, general-prover termination.  Emitted as a claim
  chain over the intermediate forms — the God1-readable shape of a REWRITE_TAC proof.
- **Soundness fix**: `match_tm` rejects bindings that capture a binder variable (silent
  captures had produced wrong rewrite candidates; no checked proof depended on one).

First import landed: **BOOL_CASES_AX** (claim chain: two clause rewrites, one classical
step).  Core native proofs: 306 → 307, all Megalodon-checked, zero regressions throughout.

## What the recorded proofs taught us

Recorded kernel proofs are `EQ_MP(one composite congruence equation, base)` with spine
length 1–2; elaboration closes type variables, so *position-level* replay is the wrong
interface — the leaf set is the right guidance, and the native side reconstructs the
chain.  The remaining failure classes, measured full-width:

1. **Conditional rewrites** (`0 ^ n = if n = 0 then 1 else 0` under case hypotheses):
   need N6 — following the recorded `DEDUCT_ANTISYM_RULE`/`ASSUME` case structure.
2. **Premise scarcity**: 1 576 of 1 901 attempts have no first rule because their leaf
   lemmas lack native proofs yet; the guided pool grows with the self-contained frontier.

## Next

N6: case-structure following (the conditional class); premise-pool growth via the
self-contained prover frontier; then the same pipeline over the standard and Multivariate
profiles once their proof recording lands (Multivariate has no imported proofs yet — its
recording is part of P1/P3).

## Addendum (same day): blocking-leaf ranking and cross-profile propagation

Ranking the leaves that block the 608 guided targets gave the priority list
(num_INDUCTION 90, ADD_CLAUSES 58, MULT_CLAUSES 47, ...).  Feeding its God1-native slice
into the builtin premise table (`neq_ordsucc_0`, `ordsucc_inj`, the `omega`/`nat_p`/`SNo`
coercions, `add/mul_SNo` closure, commutativity and units) made ADD_0, ADD_SYM, MULT_SYM
and SUC_INJ self-contained (core 311), and propagating the N5c prover across profiles
yields **343 of 4 290** on standard and the **first Multivariate native proofs: 331 of
17 138** — every emission Megalodon-checked, certification untouched (2 592 / 3 839 /
3 311 transport_checked; 847 core fully_proved).

Totals on `dev/proofs-v1` today: 985 native `Qed` proofs across the three profiles.

## Second addendum: N5d and cross-profile totals

The second God1 order/arithmetic builtin batch (`omega_nonneg`, `omega_ordsucc`,
`SNoLe_ref`, `SNoLt_irref`, `SNoLe_antisym`, `mul_SNo_zeroL`, `add/mul_SNo_assoc`) makes
the LE_0 / LE_REFL / LT_REFL / LE_ANTISYM and ADD_ASSOC / MULT_ASSOC families
self-contained across the num/int/real transfers, and the leaf-guided pool immediately
grows with them: **five guided imports** (BOOL_CASES_AX, LT_REFL, INT_LT_REFL,
REAL_LT_REFL, INT_SGN_0) — the mutual growth loop works as designed.

End of day on `dev/proofs-v1`: **core 331 / standard 359 / multivariate 348 = 1 038
native `Qed` proofs**, all Megalodon-checked; certification untouched
(2 592 / 3 839 / 3 311 transport_checked, 847 core fully_proved, 7 both).
242 of the 847 imported core theorems now carry nice native proofs; the designed next
steps are N6 (recorded case-structure following: the conditional-rewrite class and
`nat_ind`-style schemata) and the §24.2 auto-compat emitter for Multivariate.

## Third addendum: day-end landing

N5e–g landed: the nat_ind/seq_induct schema rules, the NotApp binding fix, and the
SUC/multiplication arithmetic batches.  Committed cross-profile state: **core 343 /
standard 374 / multivariate 348 = 1 065 native Qed proofs** (266 of the 847 imported core
theorems now carry nice native proofs, from 236 at the start of the goal).  The
multivariate N5g propagation pass (~11 min) was repeatedly killed in the background and
is deferred to the next session, where failed-subgoal memoization (the queued performance
item) will bring the synthesis phase back under the window; its working tree was reset to
the committed 348 state.  Next: memoization, the MULT_CLAUSES nat/SNo chain, N6
case-structure following, and Multivariate proof recording.

## Fourth addendum: the memoized loop

N5h–j: failed-subgoal memoization halves the synthesis phase (identical outputs) and
restores affordability of implication lambdas; hand-bridged lemmas in `logic.mg`
(`mul_SNo_SR/SL_omega`, `SNoLt/SNoLe_ordsucc_iff_omega`) plus the SNo transitivity
builtins land MULT_CLAUSES/MULT/MULT_SUC, NOT_IMP and the four TRANS families.
Cross-profile totals: **core 351 / standard 382 / multivariate 371 = 1 104 native Qed
proofs**; **275 of the 847 imported core theorems** now carry nice native proofs (from
236 at goal start).  The probe-fix-land loop runs at roughly +5 theorems per ~40-minute
round; the remaining 572 targets concentrate in recursors, HO choice (SKOLEM), and
case-replay classes — the N6 machinery.

## Addendum 2 (same day): the classical/choice/recursion campaign (N6a–N10)

Eight prover increments landed today, each by the probe→fix→land loop with a sentinel
sweep after every change.  N6a added the classical refutation rule (goal `False` →
apply a negation hypothesis; depth- and or-hypothesis-gated), the `xm_split` loop guard
and prop-keyed memoization: DE_MORGAN_THM, NOT_FORALL_THM and the PSUBSET family became
self-contained (core 361).  N6b's atom split (case on a propositional variable of the
goal when no negation hypothesis exists) made BOOL_CASES_AX — then the top blocker with
65 dependents — a public `Qed` (364).  N7 hand-bridged `skolem_thm` (bounded HO choice
via `choose_in` on a separation) and `num_recursion` (`nat_primrec` witness) in
`logic.mg`, made `match_tm` alpha-aware, gave `close_term` an alpha-equal hypothesis
fallback, and let bare existential goals consult `close_term`: SKOLEM_THM (33 blockers)
and num_RECURSION landed (377).  N8 built a genuine list recursor from `seq_foldr` by
the pair trick (`list_recursion`, first-try Megalodon check; 378).  N9/N10 turned the
existing finseq equation lemmas into builtin premises (APPEND, MAP, LENGTH, ALL close
immediately), added SetAdjoin/ordsucc or-derivations, numeral↔ordsucc matching, and the
real-order bridge (`real_SNo`, `real_lt_iff`, minus/exp builtins): the whole REAL_*
order/ring block fell in one cascade — core **430**, standard **462**, multivariate
**440** = **1 332** native `Qed` proofs (all Megalodon-checked; certification unchanged
at 2 592 / 3 839 / 3 311 transport_checked, 847 core fully_proved).  One latent bug was
surfaced and fixed by the standard propagation: `divides_nat` unfolds with `mul_nat`
(God1 defines it before `mul_SNo` exists), not `mul_SNo`.

Of the 847 imported core theorems, **318 (37.5 %)** now carry nice native proofs, up
from 276 this morning.  Remaining top blockers: COND_ELIM_THM (equation-hypothesis goal
transport), EXP-family aggregates, DIVISION/MOD, LE_EXISTS, EVEN, IN_ELIM_THM,
FORALL_PAIR_THM.

## Addendum 3 (2026-09-02): the theory-cluster campaign (N11–N16)

Six further increments, one per cluster, each probe-verified and landed across all
three profiles with zero losses.  N11 (minus/exp/pair builtins + `seq_ex_nil/cons`,
`pair_eta_setprod`; the fuel-sensitive LEFT/RIGHT_OR_EXISTS pair was transplanted
verbatim into logic.mg, ending that regression class for good), N12 (goal-rewrite rule
with equation hypotheses, the `if_split` condition-push repair, additive cancel/
monotonicity builtins, `cond_elim_thm`/`not_SNoLe_iff_omega`/`SNoLe_add_omega`; the
`ppp_fn` eta-expansion and the self-contained shard-emission repair both came out of
propagation failures), N13 (AC mixed-middle builtins, `not_cons_nil`, the NotApp
pre-binding matcher fix), N14 (`forall/exists_pair_thm` over the Sigma toolkit),
N15 (`right_imp_exists_thm`, `sub_0_thm`, `in_cross_thm`), N16 (`sub_suc_thm` through
God1's negative-successor bridge).  Totals: core **480**, standard **512**,
multivariate **491** = **1 483** native `Qed` proofs; **349 of 847 (41.2 %)** imported
core theorems now carry nice native proofs (318 at the start of the campaign,
276 at the start of 2026-09-01).  logic.mg holds 50 hand-bridged theorems, every one
Megalodon-checked before landing; certification is unchanged throughout
(2 592 / 3 839 / 3 311 transport_checked, 847 core fully_proved).

Remaining blocker clusters, by unlock size: DIVISION/MOD/DIVMOD_UNIQ (~30), EVEN/
NOT_EVEN (~17), real_div/REAL_MUL_LINV (~14), LE_EXISTS (11), dist (10) — each is one
or two substantial hand lemmas; and the structural lever beyond them is premise-using
synthesis (recorded-proof replay, §24.3), which would harvest the guided dependents
(ADD_SUB and kin) wholesale.
