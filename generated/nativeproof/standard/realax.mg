// Native proofs generated by hol2mg (docs/DESIGN.md 23).
// They use only the native context: God1 + native prelude + public definitions.

// HOL Light: realax.ml / real_sub
Theorem real_sub : forall x y :e R, x + - y = x + - y.
let x. assume Hx.
let y. assume Hy.
exact (fun q H => H).
Qed.

// HOL Light: realax.ml / real_ge
Theorem real_ge : forall y x :e R, y <= x <-> y <= x.
let y. assume Hy.
let x. assume Hx.
apply iffI.
- assume H1.
  exact H1.
- assume H.
  exact H.
Qed.

// HOL Light: realax.ml / real_gt
Theorem real_gt : forall y x :e R, y < x <-> y < x.
let y. assume Hy.
let x. assume Hx.
apply iffI.
- assume H1.
  exact H1.
- assume H.
  exact H.
Qed.

// HOL Light: realax.ml / real_max
Theorem real_max : forall n m :e R, (if m <= n then n else m) = if m <= n then n else m.
let n. assume Hn.
let m. assume Hm.
exact (fun q H => H).
Qed.

// HOL Light: realax.ml / real_min
Theorem real_min : forall m n :e R, (if m <= n then m else n) = if m <= n then m else n.
let m. assume Hm.
let n. assume Hn.
exact (fun q H => H).
Qed.

